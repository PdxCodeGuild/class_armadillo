contacts = [
    {
    'name': 'larry',
    'age': 3,

},
    {
    'name': 'bob',
    'age': 34,
}
]

# for val in contacts[0].values():
#     val = 'new'
print(contacts[len(contacts)-2])

# name = input('Please enter a name: ')
# age = int(input('Please enter in an age: '))
# email = input('Please enter in an email: ')
# fav_color = input('Please enter in a favorite color: ')

# contacts.append({
#     'name': name,
#     'age': age,
#     'email': email,
#     'favorite color': fav_color,
# })

# for key,val in contacts.keys():
#     key['name'] = name
#     key['age'] = age
#     key['email'] = email
#     key['favorite color'] = fav_color
# return contacts

# for key in contacts:
#     print(key)